<div class="col-sm-10 col-sm-offset-1">
	<h1><%= project.escape("title") %></h1>
	<h4><%= project.escape("description") %></h4>
	<% project.images().forEach(function(image){ %>
		<div class="float-left">
			<img src="<%= image.escape("photo_url") %>">
		</div>
	<% }) %>
</div>
	